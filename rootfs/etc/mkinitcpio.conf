# vim:set ft=sh

MODULES=(dm_mod ext4 sha256 sha512 overlay)
BINARIES=()
FILES=(/lib/firmware/edid/55OLED803_patched.bin)
HOOKS=(microcode systemd modconf kms keyboard sd-vconsole block filesystems fsck)
COMPRESSION="xz"
COMPRESSION_OPTIONS=(-v -9e)
